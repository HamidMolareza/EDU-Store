@page
@model LogoutModel
@{
    ViewData["Title"] = "خروج";
}

<header>
    @{
        if (User.Identity?.IsAuthenticated ?? false) {
            <div class="align-items-center container d-flex div_border flex-column gap-1 mt-5 mx-auto my-0 p-3 p-4 rounded-1" style="max-width: 600px">
                <h2>خروج از اکانت</h2>
                <p>آیا مطمئن هستین میخواین از اکانت خود خارج شوید؟</p>
                <div class="d-flex flex-row gap-2 mt-3">
                    <a asp-area="" asp-page="/" class="btn btn-primary">بازگشت به صفحه اصلی</a>
                    <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" class="form-inline" method="post">
                        <button class="btn btn-danger" type="submit">خروج</button>
                    </form>
                </div>
            </div>
        }
        else {
            <p>شما با موفقیت از اکانت خود خارج شده‌اید.</p>
        }
    }
</header>